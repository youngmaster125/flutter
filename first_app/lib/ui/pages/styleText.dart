import 'package:flutter/material.dart';


class Styletext extends StatelessWidget {
  const Styletext({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("My own work"),),
     body: const Center(
       child: Text("Manley"),
     ),
     // drawer: const Drawer(),
    );
  }
}
